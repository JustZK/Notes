<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.zk.notes">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".login.LoginActivity" />
        <activity android:name=".player.MediaPlayerActivity" />
        <activity
            android:name=".slidingpanelayout.SlidingPaneLayoutActivity"
            android:theme="@style/DIY.SlideClose.Transparent.Theme" />
        <activity android:name=".slidingpanelayout.SlidingPaneLayoutActivityOne" />
        <activity android:name=".slidingpanelayout.SlidingPaneLayoutActivityTwo" />
        <activity
            android:name=".slidingpanelayout.SlidingPaneLayoutActivityThree"
            android:theme="@style/DIY.SlideClose.Transparent.Theme"></activity>
    </application>

</manifest>